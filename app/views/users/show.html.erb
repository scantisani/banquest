<% @user.runs.each do |run| %>
  <% next if run.map == 'Game over!' %>
  <%= link_to "In-progress run (ID: #{run.id})", user_run_url(@user, run) %><br>
<% end %>

<%= form_for ([@user, @user.runs.build]) do |f| %>
  <%= f.submit %>
<% end %>